import { createStore } from 'vuex'

export default createStore({
  state: {
    // state管理
    // どういうデータを管理するのか
  },
  getters: {
  },
  mutations: {
  },
  actions: {
    // 非同期通信で使う
  },
  modules: {
  }
})
